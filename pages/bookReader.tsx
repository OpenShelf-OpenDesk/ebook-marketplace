import React from "react";
import BookReader from "../src/components/common/BookReader";

interface Props {}

const bookReader = (props: Props) => {
  return <BookReader />;
};

export default bookReader;
