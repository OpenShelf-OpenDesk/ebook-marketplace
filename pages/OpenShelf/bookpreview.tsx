import React from 'react';
import BookPreview from '../../src/components/OpenShelf/Views/BookPreview';

interface Props {}

const bookpreview = (props: Props) => {
  return <BookPreview />;
};

export default bookpreview;
